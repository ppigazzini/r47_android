# Android Docs

Development documentation for the Android shell lives under [dev/README.md](dev/README.md).

That subtree documents the checked-in build contract, Kotlin shell, native bridge,
UI rendering path, CI lane, and official references for people changing the app
rather than using it.

Keep those pages aligned with live Gradle files, shell scripts, and
`.github/workflows/android-ci.yml`.
